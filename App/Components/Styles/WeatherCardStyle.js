import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    margin: 20,
    height: 60,
    borderRadius: 10,
    justifyContent: 'center',
    padding: 10,
    flexDirection: 'row'
  },
  deleteBtn: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center'
  },
  tempView: {
    flex: 4
  }
});
