import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    alignItems: "center",
    justifyContent: "center",
    height: 40,
    width: 250,
    borderWidth: 1,
    borderColor: "#000",
    padding: 5,
    borderRadius: 8
  }
});
